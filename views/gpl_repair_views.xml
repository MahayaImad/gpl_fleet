<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_repair_order_form_gpl_inherit" model="ir.ui.view">
        <field name="name">repair.order.form.gpl.inherit</field>
        <field name="model">repair.order</field>
        <field name="inherit_id" ref="repair.view_repair_order_form"/>
        <field name="arch" type="xml">
            <!-- Modifier l'en-tête pour mettre en évidence le véhicule GPL -->
            <xpath expr="//field[@name='partner_id']" position="before">
                <field name="gpl_vehicle_id" options="{'no_create': True}" placeholder="Sélectionner un véhicule .."/>
            </xpath>

            <!-- Ajouter le type de réparation GPL -->
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="gpl_repair_type" widget="selection" invisible="gpl_vehicle_id == False" required="gpl_vehicle_id != False"/>
                <field name="is_gpl_repair" invisible="1"/>
            </xpath>

            <!-- Rendre le produit et le lot moins visibles pour les réparations GPL -->
            <xpath expr="//field[@name='product_id']" position="attributes">
                <attribute name="readonly">is_gpl_repair == True</attribute>
                <attribute name="string">Produit à réparer</attribute>
                <attribute name="force_save">1</attribute>
                <attribute name="invisible">1</attribute>
            </xpath>

            <xpath expr="//field[@name='product_qty']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>

            <xpath expr="//field[@name='lot_id']" position="attributes">
                <attribute name="invisible">is_gpl_repair == True</attribute>
            </xpath>

            <!-- Ajouter un nouvel onglet pour le diagnostic GPL -->
            <notebook position="inside">
                <page string="Diagnostic GPL" invisible="gpl_vehicle_id == False" >
                    <group>
                        <field name="diagnostic" placeholder="Entrez le diagnostic détaillé du problème GPL..." nolabel="1"/>
                    </group>
                </page>
            </notebook>

            <!-- Ajouter des informations d'aide -->
            <xpath expr="//sheet" position="after">
                <div class="alert alert-info" role="alert" invisible="gpl_vehicle_id == False" >
                    <p><strong>Réparation GPL</strong>: Cette réparation concerne un véhicule équipé GPL.</p>
                </div>
            </xpath>
        </field>
    </record>

    <record id="view_repair_order_tree_gpl_inherit" model="ir.ui.view">
        <field name="name">repair.order.tree.gpl.inherit</field>
        <field name="model">repair.order</field>
        <field name="inherit_id" ref="repair.view_repair_order_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position="after">
                <field name="gpl_vehicle_id" optional="show"/>
                <field name="gpl_repair_type" optional="show"/>
            </xpath>
        </field>
    </record>

    <!-- Vue recherche héritée pour repair.order -->
    <record id="view_repair_order_search_gpl_inherit" model="ir.ui.view">
        <field name="name">repair.order.search.gpl.inherit</field>
        <field name="model">repair.order</field>
        <field name="inherit_id" ref="repair.view_repair_order_form_filter"/>
        <field name="arch" type="xml">
            <!-- Ajouter le champ de recherche après partner_id -->
            <field name="partner_id" position="after">
                <field name="gpl_vehicle_id"/>
                <field name="gpl_repair_type"/>
                <filter string="Réparations GPL" name="gpl_repairs" domain="[('gpl_vehicle_id', '!=', False)]"/>
            </field>

            <!-- Ajouter le filtre de regroupement dans le groupe existant -->
            <filter name="product" position="after">
                <filter string="Véhicule GPL" name="group_vehicle" domain="[]" context="{'group_by': 'gpl_vehicle_id'}"/>
                <filter string="Type de réparation" name="group_repair_type" domain="[]" context="{'group_by': 'gpl_repair_type'}"/>
            </filter>
        </field>
    </record>

    <!-- Action de menu pour les réparations GPL -->
    <record id="action_repair_order_gpl" model="ir.actions.act_window">
        <field name="name">Réparations GPL</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">repair.order</field>
        <field name="view_mode">tree,form,kanban</field>
        <field name="domain">[('gpl_vehicle_id', '!=', False)]</field>
        <field name="context">{
            'default_is_gpl_repair': True,
            'search_default_gpl_repairs': 1
        }</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Créer votre première réparation GPL
            </p>
            <p>
                Les réparations GPL vous permettent de gérer les interventions sur les installations GPL de vos véhicules.
            </p>
        </field>
    </record>

    <!-- Vue kanban pour les réparations GPL -->
    <record id="view_repair_order_kanban_gpl" model="ir.ui.view">
        <field name="name">repair.order.kanban.gpl</field>
        <field name="model">repair.order</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile" sample="1">
                <field name="name"/>
                <field name="gpl_vehicle_id"/>
                <field name="gpl_repair_type"/>
                <field name="partner_id"/>
                <field name="state"/>
                <field name="create_date"/>
                <templates>
                    <t t-name="kanban-box">
                        <div t-attf-class="oe_kanban_global_click">
                            <div class="o_kanban_record_top mb8">
                                <div class="o_kanban_record_headings mt4">
                                    <strong class="o_kanban_record_title"><span><field name="name"/></span></strong>
                                </div>
                                <strong>
                                    <field name="state" widget="label_selection" options="{'classes': {'draft': 'warning', 'confirmed': 'success', 'under_repair': 'success', '2binvoiced': 'success', 'ready': 'success', 'cancel': 'danger'}}"/>
                                </strong>
                            </div>
                            <div class="o_kanban_record_body">
                                <span><strong>Véhicule: </strong><field name="gpl_vehicle_id"/></span><br/>
                                <span t-if="record.gpl_repair_type.raw_value"><strong>Type: </strong><field name="gpl_repair_type"/></span><br/>
                                <span><strong>Client: </strong><field name="partner_id"/></span>
                            </div>
                            <div class="o_kanban_record_bottom">
                                <div class="oe_kanban_bottom_left">
                                    <span class="oe_kanban_date"><t t-esc="record.create_date.value"/></span>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Menu pour accéder aux réparations GPL -->
    <menuitem id="menu_repair_order_gpl"
              name="Réparations GPL"
              parent="gpl_fleet.menu_gpl_operations"
              action="action_repair_order_gpl"
              sequence="20"/>
</odoo>
